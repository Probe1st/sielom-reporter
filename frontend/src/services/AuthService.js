class AuthService {
    /**
     * log in account
     * @param {object} credential user's credential 
     * @returns {object} this user
     */
    login(credential) {
        return;
    }

    /**
     * log out from account
     * @returns {object} current user
     */
    logout() {
        return;
    }

    /**
     * retrieve current user
     * @returns {object}
     */
    getCurrentUser() {
        return;
    }
}

export default AuthService();
