class OrderService {
    /**
     * get orders and return the array with them
     * @returns {object}
     */
    getOrders() {
        return;
    }

    /**
     * create the order and returns it
     * @param {object} data
     * @returns {object}
     */
    createOrder(data) {
        return;
    }

    /**
     * update the order and returns it 
     * @param {object} data 
     * @param {number} id 
     * @returns {object}
     */
    updateOrder(data, id) {
        return;
    }

    /**
     * Delete the order by id and returns it
     * @param {number} id 
     * @returns {object}
     */
    deleteOrder(id) {
        return;
    }
}

export default OrderService();
